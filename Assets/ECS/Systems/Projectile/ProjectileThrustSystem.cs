using Unity.Burst;
using Unity.Entities;

// Thrust + Fuel
// Accelerate
partial struct ProjectileThrustSystem : ISystem
{
    [BurstCompile]
    public void OnUpdate(ref SystemState state)
    {
        
    }
}
